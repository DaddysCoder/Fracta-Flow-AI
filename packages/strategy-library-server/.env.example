# Never commit a real .env file. This backend holds the key server-side
# specifically so it never reaches the client or a devtools network tab.
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-5
PORT=8787
ALLOWED_ORIGIN=http://localhost:5173
