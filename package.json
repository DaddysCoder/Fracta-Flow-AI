{
  "name": "fracta-flow-ai",
  "private": true,
  "version": "0.0.0",
  "description": "Fracta Flow AI suite — practitioner-owned, local-first PBS tooling.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "UNLICENSED"
}
